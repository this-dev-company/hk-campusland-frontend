import React from 'react';

const Dashboard = () => {
    return (
        <section className="main" id="calendar">
            <h1>Calendario</h1>
        </section>
    );

};

export default Dashboard;